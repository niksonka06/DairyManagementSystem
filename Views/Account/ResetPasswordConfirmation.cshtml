@{
    ViewData["Title"] = "Password reset";
    Layout = "_AuthLayout";
}

<div class="auth-card">
    <h2>Password updated</h2>
    <p class="lead">Your password has been changed. Sign in with your new password.</p>
    <a asp-action="Login" class="btn btn-primary w-100 py-2">Sign in</a>
</div>
